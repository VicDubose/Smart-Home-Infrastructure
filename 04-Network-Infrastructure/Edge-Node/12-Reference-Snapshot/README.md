# 12 — Reference Snapshot

This is a curated, GitHub-safe subset of the production snapshot collected on **2026-09-07** after the SSD/Jellyfin recovery.

It is **evidence**, not a promise that every captured file is directly executable restoration source. Credential-bearing lines were intentionally excluded during collection. The narrative documentation and private recovery checklist identify where authoritative private source/state is still required.

The reference set is useful for comparing a future replacement against the known-good production state.
